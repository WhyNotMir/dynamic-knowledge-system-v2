# Copy this to .env.local and fill in your values.
# When NEXT_PUBLIC_API_URL is empty, the app runs entirely on mock data.

# Base URL for the FastAPI backend (leave blank for mock mode)
NEXT_PUBLIC_API_URL=

# Example (real backend):
# NEXT_PUBLIC_API_URL=http://localhost:8000
